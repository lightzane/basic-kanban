import { MatDialogConfig } from "@angular/material/dialog";

export const DIALOG_CONFIG: MatDialogConfig = {
    width: '500px',
    maxHeight: '100vh',
    hasBackdrop: true,
    position: {
        top: '100px'
    },
    autoFocus: true
};